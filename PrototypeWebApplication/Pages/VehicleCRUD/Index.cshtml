@page
@model PrototypeWebApplication.Pages.VehicleCRUD.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle[0].Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle[0].Vin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle[0].Plate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle[0].Capacity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle[0].Driver)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Vehicle) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Capacity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Driver.Userid)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Vehicleid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Vehicleid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Vehicleid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>


<div class="text-center">
    <a href="/Index" Class="btn btn-dark"> Return Home </a>
</div> 