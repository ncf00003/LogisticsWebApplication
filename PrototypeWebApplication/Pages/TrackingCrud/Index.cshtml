@page
@model PrototypeWebApplication.Pages.TrackingCrud.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
   
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tracking[0].CurrentLocation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tracking[0].OrderDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tracking[0].LastUpdated)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tracking[0].Shipment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Tracking) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentLocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdated)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Shipment.Shipmentid)
            </td>
            <td>
              
                <a asp-page="./Details" asp-route-id="@item.Trackingid">Details</a>
                
            </td>
        </tr>
}
    </tbody>
</table>


<div class="text-center">
    <a href="/Index" Class="btn btn-dark"> Return Home </a>
</div> 